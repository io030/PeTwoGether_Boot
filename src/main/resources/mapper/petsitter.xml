<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="petsitter">

   <select id="auth" parameterType="com.test.petwogether.model.petsitter.PetSitterDTO" resultType="com.test.petwogether.model.petsitter.PetSitterDTO">
      select * from tblPetSitter where psid=#{psid} and pspw=#{pspw}
   </select>

</mapper>